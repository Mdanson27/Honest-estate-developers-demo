function invoices(){return head('FINANCE','Billing & Invoices','Recurring rent, service charges, due dates and payment status.',`<button class="btn btn-primary" onclick="toast('Invoice generated','A new recurring invoice would be created using the active lease billing profile.')">＋ Generate Invoice</button>`)+`<div class="grid g4">${metric('Invoices',state.invoices.length)}${metric('Paid',state.invoices.filter(i=>i.status==='Paid').length,'','green')}${metric('Overdue',state.invoices.filter(i=>i.status==='Overdue').length,'','red')}${metric('Outstanding',compact(state.invoices.reduce((s,i)=>s+i.balance,0)),'','red')}</div>${invoiceTable()}`}
function invoiceTable(){return `<div class="table-wrap" style="margin-top:14px"><table><thead><tr><th>Invoice</th><th>Tenant</th><th>Period</th><th>Due</th><th>Total</th><th>Paid</th><th>Balance</th><th>Status</th></tr></thead><tbody>${state.invoices.map(i=>`<tr><td><b>${i.id}</b></td><td>${tBy(i.tenantId).business}</td><td>${i.period}</td><td>${date(i.due)}</td><td>${fmt(i.total)}</td><td style="color:var(--green)">${fmt(i.paid)}</td><td style="font-weight:950;color:${i.balance?'var(--red)':'inherit'}">${fmt(i.balance)}</td><td>${status(i.status)}</td></tr>`).join('')}</tbody></table></div>`}
